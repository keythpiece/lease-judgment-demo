# Streamlit Community Cloud の App settings > Secrets に貼り付ける例です。
# サンプルデモだけなら rule_based のままでAPIキーは不要です。

LLM_PROVIDER = "rule_based"
OCR_ENGINE = "none"
DEMO_MODE = "true"
OPENAI_MODEL = "gpt-5-mini"

# OpenAI（検証用）を使う場合だけ設定してください。
OPENAI_API_KEY = ""
